import { ArrowRight,Check,Clock,Phone,ShieldCheck } from 'lucide-react'
import { Link,useSearchParams } from 'react-router-dom'
import Header from '../components/Header'

export default function RequestConfirmed(){const [params]=useSearchParams();return <div className="app-shell"><Header compact/><main className="confirmation-page"><div className="confirmation-card"><span className="confirmation-card__check"><Check/></span><div className="eyebrow">Request received</div><h1>We’ll take it from here.</h1><p>Your MitDir coordinator will review the request and call to confirm the details, permissions and estimated cost.</p><div className="confirmation-reference"><span>Reference</span><strong>{params.get('ref')||'Received'}</strong></div><div className="confirmation-steps"><div><Clock/><span><strong>Usually within 2 hours</strong>We will contact you during service hours.</span></div><div><ShieldCheck/><span><strong>No payment taken</strong>Costs are confirmed before the booking.</span></div></div><div className="confirmation-actions"><Link className="button" to="/register">Create family account<ArrowRight/></Link><a href="tel:+497211234567"><Phone/>Call concierge</a></div></div></main></div>}
